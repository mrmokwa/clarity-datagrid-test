import { PedvendSituacaoPipe } from './pedvend-situacao.pipe';

describe('PedvendSituacaoPipe', () => {
  it('create an instance', () => {
    const pipe = new PedvendSituacaoPipe();
    expect(pipe).toBeTruthy();
  });
});
