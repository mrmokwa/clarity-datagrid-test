import { SelecaoDiasPipe } from './selecao-dias.pipe';

describe('SelecaoDiasPipe', () => {
  it('create an instance', () => {
    const pipe = new SelecaoDiasPipe();
    expect(pipe).toBeTruthy();
  });
});
